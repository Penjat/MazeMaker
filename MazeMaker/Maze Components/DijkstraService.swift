import Foundation

class DijkstraService {
    func findPath(at: CellLocation, value: Int, mazeProvider: SquareMaze) {
        for neighbor in mazeProvider.freeNeighbors(at) {
            neighbor
        }
    }
}
